name: Build Docker Image

on:
  push:
    branches:
      - s

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Docker image
      run: docker build -t my-python-app .
